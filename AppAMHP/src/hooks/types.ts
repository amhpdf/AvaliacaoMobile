export interface IBanner {
    id: number;
  }

  export interface INews {
    id: number;
    titulo: string;
    imagem: string;
    dataPublicacao: string;
  }

  export interface IConvenio {
    id: number;
    nomeConvenio: string;
  }
